# このリポジトリの作業ルール（onigomex/dotfiles）

## 公開リポジトリ
- このリポジトリは **public** です。トークン・鍵・認証情報などのセンシティブデータをコミットしないでください。
- コミット前に必ず差分をスキャンし、機密が含まれないことを確認してください。

## 変更の反映は必ず PR 経由
- `main` へ直接コミット／push しないでください。
- 変更は必ず branch → PR で行ってください（容易に revert できるようにするため）。
- **マージはユーザーが行います。** public リポジトリのため、Claude は PR 作成までとし、マージ前にユーザーの最終確認を必須とします。
- ユーザーがマージしたら、Claude は main への反映を確認し、マージ済みブランチを掃除してください（マージ漏れ防止）。

## ロールの追加・削除
- ロール（`roles/<ROLE>/`）を追加・削除したら、`roles.lst` を必ず更新してください（忘れがち）。
- 更新を忘れると、`make install`（`ROLE=` 無しの一括インストール）の対象から漏れます。
